# PlainSpeak Language for VS Code

This extension adds syntax highlighting and basic bracket/quote handling for
PlainSpeak `.eng` source files. It is declarative TextMate grammar only; the
compiler remains the source of truth for parsing and diagnostics.

## Local installation

Open this directory in VS Code and press `F5`, or package it with `vsce
package` after installing the VS Code Extension Manager.
