Say Choose 10 when 1 otherwise 20. Say Choose 10 when 0 otherwise 20. Declare x as integer with value 7. Declare p as pointer to integer with value Address of x. Say Choose 1 when p otherwise 2. Declare cp as pointer to constant integer with value p. Declare selected as pointer to constant integer with value Choose p when 1 otherwise cp. Say Value at selected. Declare vp as pointer to void with value Convert p to type pointer to void. Declare selectedVoid as pointer to void with value Choose p when 0 otherwise vp. Say Convert selectedVoid to type boolean. Structure point: Field x as integer. End structure. Declare a as structure point with members x as 3 done. Declare b as structure point with members x as 4 done. Declare c as structure point with value Choose a when 1 otherwise b. Say Member x of c.
