#pragma once
#include <string>

bool isSetKeyword(const std::string &w);
bool isSayKeyword(const std::string &w);
bool isDeclareKeyword(const std::string &w);
bool isAddKeyword(const std::string &w);
bool isSubtractKeyword(const std::string &w);
bool isIncreaseKeyword(const std::string &w);
bool isDecreaseKeyword(const std::string &w);
bool isAppendKeyword(const std::string &w);
bool isReturnKeyword(const std::string &w);
bool isUnlessKeyword(const std::string &w);
bool isUntilKeyword(const std::string &w);